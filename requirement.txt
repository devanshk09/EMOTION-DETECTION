# For backend (Flask, model inference, etc.)
Flask
flask-cors
gunicorn
requests
transformers
torch
torchvision
numpy
pandas

# For frontend (if building / bundling via Python)
# (Often handled via npm/yarn for React, so these may not be needed)
# But if there is any Python tool integration, you may include e.g.
Flask-Compress
python-dotenv
